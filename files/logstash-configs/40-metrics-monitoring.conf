filter {
  if [type] == "filesystem" {
    if [fs][used_p] >= 0.8 {
      mutate {
        add_tag => "riemann"
        replace => { "message" => "Disk utilization is above 80%" }
      }
    }
  }
}

